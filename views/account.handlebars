<div>
  <h2>User Account</h2>
  <p>User ID: {{userId}}</p>
  <p>Name: {{name}}</p>
  <p>Email: {{email}}</p>
  <p>Phone Number: {{phoneNumber}}</p>
  <p>Address: {{address}}</p>
  <p>Books Checked Out:</p>
  <ul>
    {{#each bookarr as |book|}}
      <li>{{book.id}} - {{book.title}} (Due Date: {{dueDate}})</li>
    {{/each}}
  </ul>
  <style>
body {
  background-color: tan;
}
</div>
<script src="/js/checkout.js"></script>
