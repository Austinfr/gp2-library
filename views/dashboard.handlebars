
<h1>Library Dashboard</h1>
<table>
  <thead>
  <tbody>
    <tr>
      {{#each books}}
      <div class="book">
        <h2>{{title}}</h2>
        <p>{{author}}</p>
        <p>{{description}}</p>
      </div>
      {{/each}}
    </tr>
  </tbody>
  </thead>
</table>

